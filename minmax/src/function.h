#ifndef FUNCTION_H
#define FUNCTION_H

#include <vector>
#include <utility>
using std::pair;

namespace function  // não é necessário definir um namespace
{
  std::pair<int, int> min_max(int V[], std::size_t n);
}

#endif
